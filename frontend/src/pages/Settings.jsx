import { useState } from 'react';
import { useAuth } from '../context/AuthContext';
import WorkspaceShell from '../components/WorkspaceShell';

export default function Settings() { const { logout } = useAuth(); const [saved, setSaved] = useState(false); return <WorkspaceShell title="Settings" eyebrow="Account preferences"><div className="mt-8 max-w-2xl space-y-6"><section className="card p-6"><h2 className="font-display text-2xl text-ink-900 dark:text-white">Notifications</h2><label className="mt-5 flex items-center justify-between text-sm text-ink-700 dark:text-ink-200">Email notifications<input className="h-5 w-5 accent-ember-500" type="checkbox" defaultChecked /></label><label className="mt-4 flex items-center justify-between text-sm text-ink-700 dark:text-ink-200">Session reminders<input className="h-5 w-5 accent-ember-500" type="checkbox" defaultChecked /></label><button className="btn-primary mt-6" onClick={() => setSaved(true)}>Save preferences</button>{saved && <p className="mt-3 text-sm text-green-700">Preferences saved.</p>}</section><section className="card border-red-200 p-6"><h2 className="font-display text-2xl text-red-800">Session</h2><p className="mt-2 text-sm text-ink-500">Sign out of this device.</p><button className="btn-secondary mt-5" onClick={logout}>Log out</button></section></div></WorkspaceShell>; }
